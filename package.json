{
  "name": "schedule-platform",
  "version": "1.0.0",
  "private": true,
  "description": "منصة توليد جدول الدورات التدريبية الأسبوعي",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "node scripts/build.js",
    "start": "npx serve dist -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
